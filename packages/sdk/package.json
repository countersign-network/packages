{
  "name": "@countersign/sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@countersign/api-contract": "workspace:*"
  },
  "devDependencies": {
    "@countersign/api": "workspace:*",
    "@countersign/core": "workspace:*",
    "@countersign/policy": "workspace:*",
    "@countersign/provider-mock": "workspace:*"
  },
  "comment": "Tier 0 #4 (front door): the typed client agents/operators use to wire Countersign in. Browser + Node (global fetch/WebSocket). The Dart client generates from api-contract; this is its TS twin."
}
