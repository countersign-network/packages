{
  "name": "@countersign/x402",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@countersign/api-contract": "workspace:*",
    "@countersign/policy": "workspace:*"
  },
  "devDependencies": {
    "@countersign/api": "workspace:*",
    "@countersign/core": "workspace:*",
    "@countersign/provider-mock": "workspace:*"
  },
  "comment": "Governs x402 (the HTTP 402 machine-payment rail) as a first-class Countersign action: parse a payment challenge -> route through the spend guard -> only pay if policy allows. Countersign governs; it never moves funds (prime directive #1)."
}
