{
  "name": "@cosign/x402",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@cosign/api-contract": "workspace:*",
    "@cosign/policy": "workspace:*"
  },
  "devDependencies": {
    "@cosign/api": "workspace:*",
    "@cosign/core": "workspace:*",
    "@cosign/provider-mock": "workspace:*"
  },
  "comment": "Governs x402 (the HTTP 402 machine-payment rail) as a first-class Cosign action: parse a payment challenge -> route through the spend guard -> only pay if policy allows. Cosign governs; it never moves funds (prime directive #1)."
}
